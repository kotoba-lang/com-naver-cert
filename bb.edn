{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([naver_cert.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'naver_cert.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
